# Copy to Cloudflare Pages → Settings → Environment variables (Production).
# Values are baked in at build time for static export.

# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX)
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Google Search Console HTML-tag verification content (optional if using DNS TXT)
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
